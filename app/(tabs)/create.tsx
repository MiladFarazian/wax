/**
 * Placeholder for the center Create (+) tab. It never actually renders: the tab
 * bar intercepts the press and opens the /new-post modal instead (see _layout).
 * The file exists only so expo-router registers the tab slot.
 */
export default function CreateTab() {
  return null;
}
